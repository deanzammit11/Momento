<?xml version="1.0" encoding="utf-8"?>
<!-- Container for Manage Categories screen -->
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/light_teal">

    <!-- Manage Categories Heading -->
    <TextView
        android:id="@+id/tvManageCategoriesTitle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Manage Categories"
        android:textSize="32sp"
        android:textStyle="bold"
        android:textColor="@color/dark_text"
        android:gravity="center"
        android:layout_marginTop="50dp"/>

    <!-- Add Category Button -->
    <Button
        android:id="@+id/buttonAddCategory"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="+ Add Category"
        android:textColor="@android:color/white"
        android:backgroundTint="@color/dark_blue"
        android:paddingHorizontal="24dp"
        android:paddingVertical="12dp"
        android:layout_below="@id/tvManageCategoriesTitle"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="16dp"/>

    <!-- RecyclerView for listing all categories -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recycler_view_categories"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@id/buttonAddCategory"
        android:layout_marginTop="16dp"
        android:layout_marginBottom="80dp"/>
</RelativeLayout>
